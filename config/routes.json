{
  "routes": [
    {
      "method": "POST",
      "path": "/api/categories",
      "handler": "user.create",
      "config": {
        "policies": ["otherMiddleware"]
      }
    }
  ]
}
